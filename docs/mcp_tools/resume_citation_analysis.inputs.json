{
  "type": "object",
  "properties": {
    "job_id": {
      "type": "string",
      "description": "The job ID (short UUID) from a previous analyze_citations call."
    },
    "wait": {
      "type": "boolean",
      "description": "If true, the server sleeps through the upstream rate-limit window (capped at ~90s) and retries automatically. Use this after a previous call reported a short wait_until. Default false: the tool returns immediately on rate limit so you can tell the user before waiting.",
      "default": false
    }
  },
  "required": [
    "job_id"
  ],
  "additionalProperties": false
}
