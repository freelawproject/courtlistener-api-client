{
  "type": "object",
  "properties": {
    "query_id": {
      "type": "string",
      "description": "The query ID (short UUID) from a previous search or call_endpoint result."
    },
    "num_results": {
      "type": "integer",
      "description": "Number of results to return (1-100). Defaults to 20.",
      "minimum": 1,
      "maximum": 100,
      "default": 20
    }
  },
  "required": [
    "query_id"
  ],
  "additionalProperties": false
}
