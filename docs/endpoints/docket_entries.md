<!-- Auto-generated by scripts/generate_endpoint_docs.py. Do not edit. -->

# `docket_entries`

**Docket Entries**

- **Path:** `/docket-entries/`
- **Endpoint ID:** `docket-entries`
- **Source:** `courtlistener/models/endpoints/docket_entries.py`
- **Fields:** 11

## Description

Docket Entries Endpoint

## Fields

### `fields`

string | array of string

Filter which fields are returned.

<details><summary>Choices (13)</summary>

| Value | Label |
|---|---|
| `"resource_uri"` | Resource uri |
| `"id"` | Id |
| `"docket"` | Docket |
| `"recap_documents"` | Recap documents |
| `"date_created"` | Date created |
| `"date_modified"` | Date modified |
| `"date_filed"` | Date filed |
| `"time_filed"` | Time filed |
| `"entry_number"` | Entry number |
| `"recap_sequence_number"` | Recap sequence number |
| `"pacer_sequence_number"` | Pacer sequence number |
| `"description"` | Description |
| `"tags"` | Tags |

</details>

### `id`

integer

Lookups: `gt`, `gte`, `lt`, `lte`, `range`

### `entry_number`

integer

# on the PACER docket page. For appellate cases, this may be the internal PACER ID for the document, when an entry ID is otherwise unavailable.

Lookups: `gt`, `gte`, `isnull`, `lt`, `lte`, `range`

### `date_created`

string (date-time)

The moment when the item was created.

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_modified`

string (date-time)

The last moment when the item was modified. A value in year 1750 indicates the value is unknown

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_filed`

string (date)

The created date of the Docket Entry according to the court timezone.

Lookups: `day`, `gt`, `gte`, `lt`, `lte`, `month`, `range`, `year`

### `pacer_sequence_number`

integer

The de_seqno value pulled out of dockets, RSS feeds, and sundry other pages in PACER. The place to find this is currently in the onclick attribute of the links in PACER. Because we do not have this value for all items in the DB, we do not use this value for anything. Still, we collect it for good measure.

Lookups: `gt`, `gte`, `isnull`, `lt`, `lte`, `range`

### `docket`

integer

Related filter: pass a dockets record's id, or a dict of dockets sub-filters (sent as docket__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`dockets`](./dockets.md)

### `recap_documents`

integer

Related filter: pass a recap-documents record's id, or a dict of recap-documents sub-filters (sent as recap_documents__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`recap_documents`](./recap_documents.md)

### `tags`

integer

The tags associated with the docket entry.

Related endpoint: [`tags`](./tags.md)

### `order_by`

string

<details><summary>Choices (12)</summary>

| Value | Label |
|---|---|
| `"id"` | Id (asc) |
| `"-id"` | Id (desc) |
| `"date_created"` | Date Created (asc) |
| `"-date_created"` | Date Created (desc) |
| `"date_modified"` | Date Modified (asc) |
| `"-date_modified"` | Date Modified (desc) |
| `"date_filed"` | Date Filed (asc) |
| `"-date_filed"` | Date Filed (desc) |
| `"recap_sequence_number"` | Recap Sequence Number (asc) |
| `"-recap_sequence_number"` | Recap Sequence Number (desc) |
| `"entry_number"` | Entry Number (asc) |
| `"-entry_number"` | Entry Number (desc) |

</details>
