<!-- Auto-generated by scripts/update_endpoint_docs.py. Do not edit. -->

# `audio`

**Audio**

- **Path:** `/audio/`
- **Endpoint ID:** `audio`
- **Source:** `courtlistener/models/endpoints/audio.py`
- **Fields:** 12

## Description

Audio Endpoint

## Fields

### `fields`

string | array of string

Filter which fields are returned.

<details><summary>Choices (25)</summary>

| Value | Label |
|---|---|
| `"resource_uri"` | Resource uri |
| `"id"` | Id |
| `"absolute_url"` | Absolute url |
| `"panel"` | Panel |
| `"docket"` | Docket |
| `"date_created"` | Date created |
| `"date_modified"` | Date modified |
| `"source"` | Source |
| `"case_name_short"` | Case name short |
| `"case_name"` | Case name |
| `"case_name_full"` | Case name full |
| `"judges"` | Judges |
| `"sha1"` | Sha1 |
| `"download_url"` | Download url |
| `"local_path_mp3"` | Local path mp3 |
| `"local_path_original_file"` | Local path original file |
| `"filepath_ia"` | Filepath ia |
| `"ia_upload_failure_count"` | Ia upload failure count |
| `"duration"` | Duration |
| `"processing_complete"` | Processing complete |
| `"date_blocked"` | Date blocked |
| `"blocked"` | Blocked |
| `"stt_status"` | Speech to text status |
| `"stt_source"` | Speech to text source |
| `"stt_transcript"` | Speech to text transcription |

</details>

### `id`

integer

Lookups: `gt`, `gte`, `lt`, `lte`, `range`

### `date_modified`

string (date-time)

The last moment when the item was modified. A value in year 1750 indicates the value is unknown

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_created`

string (date-time)

The moment when the item was created.

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `sha1`

string

unique ID for the document, as generated via SHA1 of the binary file or text data

### `blocked`

boolean

Should this item be blocked from indexing by search engines?

### `date_blocked`

string (date)

The date that this opinion was blocked from indexing by search engines

Lookups: `day`, `gt`, `gte`, `lt`, `lte`, `month`, `range`, `year`

### `processing_complete`

boolean

Is audio for this item done processing?

### `stt_status`

integer

The status of the Speech to Text for this item?

Lookups: `gt`, `gte`, `lt`, `lte`, `range`

Choices (6):

| Value | Label |
|---|---|
| `0` | Speech to Text Needed |
| `1` | Speech to Text Complete |
| `2` | Speech to Text Failed |
| `3` | Transcription does not match audio |
| `4` | File size is bigger than 25 MB |
| `5` | File does not exist |

### `docket`

integer

Related filter: pass a dockets record's id, or a dict of dockets sub-filters (sent as docket__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`dockets`](./dockets.md)

### `source`

string | array of string

the source of the audio file, one of: C (court website), H (brad heath archive)

Choices (2):

| Value | Label |
|---|---|
| `"C"` | court website |
| `"H"` | brad heath archive |

### `order_by`

string

Choices (8):

| Value | Label |
|---|---|
| `"id"` | Id (asc) |
| `"-id"` | Id (desc) |
| `"date_created"` | Date Created (asc) |
| `"-date_created"` | Date Created (desc) |
| `"date_modified"` | Date Modified (asc) |
| `"-date_modified"` | Date Modified (desc) |
| `"date_blocked"` | Date Blocked (asc) |
| `"-date_blocked"` | Date Blocked (desc) |
