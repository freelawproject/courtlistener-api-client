<!-- Auto-generated by scripts/update_endpoint_docs.py. Do not edit. -->

# `recap_documents`

**Recap Documents**

- **Path:** `/recap-documents/`
- **Endpoint ID:** `recap-documents`
- **Source:** `courtlistener/models/endpoints/recap_documents.py`
- **Fields:** 15

## Description

Recap Documents Endpoint

## Fields

### `fields`

string | array of string

Filter which fields are returned.

<details><summary>Choices (26)</summary>

| Value | Label |
|---|---|
| `"resource_uri"` | Resource uri |
| `"id"` | Id |
| `"tags"` | Tags |
| `"absolute_url"` | Absolute url |
| `"date_created"` | Date created |
| `"date_modified"` | Date modified |
| `"sha1"` | Sha1 |
| `"page_count"` | Page count |
| `"file_size"` | File size |
| `"filepath_local"` | Filepath local |
| `"filepath_ia"` | Filepath ia |
| `"ia_upload_failure_count"` | Ia upload failure count |
| `"thumbnail"` | Thumbnail |
| `"thumbnail_status"` | Thumbnail status |
| `"plain_text"` | Plain text |
| `"ocr_status"` | Ocr status |
| `"date_upload"` | Date upload |
| `"document_number"` | Document number |
| `"attachment_number"` | Attachment number |
| `"pacer_doc_id"` | Pacer doc id |
| `"is_available"` | Is available |
| `"is_free_on_pacer"` | Is free on pacer |
| `"is_sealed"` | Is sealed |
| `"document_type"` | Document type |
| `"description"` | Description |
| `"acms_document_guid"` | Acms document guid |

</details>

### `id`

integer

### `date_created`

string (date-time)

The moment when the item was created.

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_modified`

string (date-time)

The last moment when the item was modified. A value in year 1750 indicates the value is unknown

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_upload`

string (date-time)

upload_date in RECAP. The date the file was uploaded to RECAP. This information is provided by RECAP.

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `document_type`

integer

Whether this is a regular document or an attachment.

Choices (2):

| Value | Label |
|---|---|
| `1` | PACER Document |
| `2` | Attachment |

### `document_number`

string

If the file is a document, the number is the document_number in RECAP docket.

Lookups: `gt`, `gte`, `lt`, `lte`

### `pacer_doc_id`

string | array of string

The ID of the document in PACER.

### `is_available`

boolean

True if the item is available in RECAP

### `sha1`

string

The ID used for a document in RECAP

### `ocr_status`

integer

The status of OCR processing on this item.

Choices (4):

| Value | Label |
|---|---|
| `1` | OCR Complete |
| `2` | OCR Not Necessary |
| `3` | OCR Failed |
| `4` | OCR Needed |

### `is_free_on_pacer`

boolean

Is this item freely available as an opinion on PACER?

### `docket_entry`

integer

Related filter: pass a docket-entries record's id, or a dict of docket-entries sub-filters (sent as docket_entry__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`docket_entries`](./docket_entries.md)

### `tags`

integer

Related filter: pass a tags record's id, or a dict of tags sub-filters (sent as tags__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`tags`](./tags.md)

### `order_by`

string

Choices (8):

| Value | Label |
|---|---|
| `"id"` | Id (asc) |
| `"-id"` | Id (desc) |
| `"date_created"` | Date Created (asc) |
| `"-date_created"` | Date Created (desc) |
| `"date_modified"` | Date Modified (asc) |
| `"-date_modified"` | Date Modified (desc) |
| `"date_upload"` | Date Upload (asc) |
| `"-date_upload"` | Date Upload (desc) |
