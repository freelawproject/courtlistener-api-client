<!-- Auto-generated by scripts/update_endpoint_docs.py. Do not edit. -->

# `reimbursements`

**Reimbursements**

- **Path:** `/reimbursements/`
- **Endpoint ID:** `reimbursements`
- **Source:** `courtlistener/models/endpoints/reimbursements.py`
- **Fields:** 12

## Description

Reimbursements Endpoint

## Fields

### `fields`

string | array of string

Filter which fields are returned.

<details><summary>Choices (11)</summary>

| Value | Label |
|---|---|
| `"resource_uri"` | Resource uri |
| `"id"` | Id |
| `"date_created"` | Date created |
| `"date_modified"` | Date modified |
| `"source"` | Source |
| `"date_raw"` | Date raw |
| `"location"` | Location |
| `"purpose"` | Purpose |
| `"items_paid_or_provided"` | Items paid or provided |
| `"redacted"` | Redacted |
| `"financial_disclosure"` | Financial disclosure |

</details>

### `id`

integer

Lookups: `gt`, `gte`, `lt`, `lte`, `range`

### `date_created`

string (date-time)

The moment when the item was created.

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_modified`

string (date-time)

The last moment when the item was modified. A value in year 1750 indicates the value is unknown

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `redacted`

boolean

Does the reimbursement contain redaction(s)?

### `date_raw`

string

Dates as a text string for the date of reimbursements. This is often conference dates (ex. June 2-6, 2011).

Lookups: `iexact`, `istartswith`, `startswith`

### `location`

string

Location of the reimbursement (ex. Harvard Law School, Cambridge, MA).

Lookups: `iexact`, `istartswith`, `startswith`

### `source`

string

Source of the reimbursement (ex. FSU Law School).

Lookups: `iexact`, `istartswith`, `startswith`

### `purpose`

string

Purpose of the reimbursement (ex. Baseball announcer).

Lookups: `iexact`, `istartswith`, `startswith`

### `items_paid_or_provided`

string

Items reimbursed (ex. Room, Airfare).

Lookups: `iexact`, `istartswith`, `startswith`

### `financial_disclosure`

integer

The financial disclosure associated with this reimbursement.

Related endpoint: [`financial_disclosures`](./financial_disclosures.md)

### `order_by`

string

Choices (6):

| Value | Label |
|---|---|
| `"id"` | Id (asc) |
| `"-id"` | Id (desc) |
| `"date_created"` | Date Created (asc) |
| `"-date_created"` | Date Created (desc) |
| `"date_modified"` | Date Modified (asc) |
| `"-date_modified"` | Date Modified (desc) |
