<!-- Auto-generated by scripts/update_endpoint_docs.py. Do not edit. -->

# `recap_query`

**Recap Query**

- **Path:** `/recap-query/`
- **Endpoint ID:** `recap-query`
- **Source:** `courtlistener/models/endpoints/recap_query.py`
- **Fields:** 14

## Description

Recap Query Endpoint

## Fields

### `fields`

string | array of string

Filter which fields are returned.

Choices (4):

| Value | Label |
|---|---|
| `"pacer_doc_id"` | Pacer doc id |
| `"filepath_local"` | Filepath local |
| `"acms_document_guid"` | Acms document guid |
| `"id"` | ID |

### `id`

integer

### `date_created`

string (date-time)

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_modified`

string (date-time)

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `date_upload`

string (date-time)

Lookups: `day`, `gt`, `gte`, `hour`, `lt`, `lte`, `minute`, `month`, `range`, `second`, `year`

### `document_type`

integer

Choices (2):

| Value | Label |
|---|---|
| `1` | PACER Document |
| `2` | Attachment |

### `document_number`

string

Lookups: `gt`, `gte`, `lt`, `lte`

### `pacer_doc_id`

string | array of string

The ID of the document in PACER.

### `is_available`

boolean

### `sha1`

string

### `ocr_status`

integer

Choices (4):

| Value | Label |
|---|---|
| `1` | OCR Complete |
| `2` | OCR Not Necessary |
| `3` | OCR Failed |
| `4` | OCR Needed |

### `is_free_on_pacer`

boolean

### `docket_entry`

integer

Related filter: pass a docket-entries record's id, or a dict of docket-entries sub-filters (sent as docket_entry__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`docket_entries`](./docket_entries.md)

### `tags`

integer

Related filter: pass a tags record's id, or a dict of tags sub-filters (sent as tags__<subfilter> lookups). Not an enumerated choice field.

Related endpoint: [`tags`](./tags.md)
